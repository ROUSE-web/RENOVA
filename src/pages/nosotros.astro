---
import BaseLayout from "../layouts/BaseLayout.astro";
import Hero from "../components/Hero.astro";
import Icon from "../components/Icon.astro";
---

<BaseLayout
  title="Nosotros | RENOVA"
  description="Conoce más sobre Estudio Creativo Renova - Nuestra misión, visión y valores institucionales"
>
  <div class="stack gap-20">
    <main class="wrapper about">
      <div class="about-header-container">
        <Hero
          title="Acerca de Nosotros"
          tagline="En Estudio Creativo Renova somos un equipo creativo dedicado a transformar ideas en soluciones visuales estratégicas."
          align="center"
        />

        <!-- Sección Principal -->
        <section class="about-intro">
          <div class="intro-content">
            <p class="intro-text">
              Nos especializamos en el diseño gráfico, la identidad visual y la
              comunicación publicitaria, ayudando a marcas, emprendimientos y
              empresas a proyectar una imagen profesional, coherente y
              memorable.
            </p>
          </div>
        </section>
      </div>

      <!-- Misión y Visión -->
      <section class="mission-vision-section">
        <div class="mv-grid">
          <!-- Misión -->
          <article class="mv-card mission-card" data-animate="fade-up">
            <div class="mv-icon">
              <Icon icon="rocket-launch" size="2.5rem" gradient />
            </div>
            <div class="mv-content">
              <h2 class="mv-title">Misión</h2>
              <p class="mv-text">
                Enfocarnos en crear marcas con alma y propósito, transformando
                ideas en identidades visuales sólidas que inspiran confianza,
                generan conexión y fortalecen el crecimiento de empresas y
                emprendimientos en el mundo digital.
              </p>
              <p class="mv-text">
                Ser un estudio creativo referente en diseño e identidad visual,
                reconocido por crear marcas auténticas y estratégicas que
                inspiran, conectan y aportan valor, fortaleciendo el crecimiento
                de empresas pequeñas, medianas y grandes además de los pequeños
                emprendimientos.
              </p>
            </div>
          </article>

          <!-- Visión -->
          <article class="mv-card vision-card" data-animate="fade-up">
            <div class="mv-icon">
              <Icon icon="lightbulb" size="2.5rem" gradient />
            </div>
            <div class="mv-content">
              <h2 class="mv-title">Visión</h2>
              <p class="mv-text">
                Buscamos revolucionar y desarrollar marcas visionarias, para
                empresas que valoran la creatividad, la calidad y el impacto en
                su comunicación visual.
              </p>
              <p class="mv-text">
                Transformamos proyectos en historias de éxito y construimos un
                legado de diseño innovador que perdure en el tiempo.
              </p>
            </div>
          </article>
        </div>
      </section>

      <!-- Valores Institucionales -->
      <section class="values-section">
        <div class="values-header" data-animate="fade-up">
          <h2 class="section-title">Nuestros Valores</h2>
          <p class="section-subtitle">
            Los valores y pilares fundamentales en cada proyecto que realizamos.
          </p>
        </div>

        <div class="values-grid">
          <article class="value-card" data-animate="fade-up" data-delay="0">
            <div class="value-icon-wrapper">
              <div class="value-icon">
                <Icon icon="palette" size="2rem" />
              </div>
            </div>
            <h3 class="value-title">Creatividad</h3>
            <p class="value-description">
              Exploramos nuevas ideas y enfoques únicos para cada proyecto,
              rompiendo moldes y creando soluciones originales.
            </p>
          </article>

          <article class="value-card" data-animate="fade-up" data-delay="1">
            <div class="value-icon-wrapper">
              <div class="value-icon">
                <Icon icon="heart" size="2rem" />
              </div>
            </div>
            <h3 class="value-title">Pasión</h3>
            <p class="value-description">
              Amamos lo que hacemos y eso se refleja en cada detalle de nuestro
              trabajo.
            </p>
          </article>

          <article class="value-card" data-animate="fade-up" data-delay="2">
            <div class="value-icon-wrapper">
              <div class="value-icon">
                <Icon icon="strategy" size="2rem" />
              </div>
            </div>
            <h3 class="value-title">Innovación</h3>
            <p class="value-description">
              Nos mantenemos a la vanguardia de las tendencias para ofrecer
              soluciones modernas y efectivas.
            </p>
          </article>

          <article class="value-card" data-animate="fade-up" data-delay="3">
            <div class="value-icon-wrapper">
              <div class="value-icon">
                <Icon icon="trophy" size="2rem" />
              </div>
            </div>
            <h3 class="value-title">Calidad</h3>
            <p class="value-description">
              Cada proyecto recibe nuestra máxima atención y compromiso con la
              excelencia.
            </p>
          </article>

          <article class="value-card" data-animate="fade-up" data-delay="4">
            <div class="value-icon-wrapper">
              <div class="value-icon">
                <Icon icon="paper-plane-tilt" size="2rem" />
              </div>
            </div>
            <h3 class="value-title">Responsabilidad</h3>
            <p class="value-description">
              Cumplimos con nuestros compromisos y entregamos resultados que
              superan expectativas.
            </p>
          </article>

          <article class="value-card" data-animate="fade-up" data-delay="5">
            <div class="value-icon-wrapper">
              <div class="value-icon">
                <Icon icon="star" size="2rem" />
              </div>
            </div>
            <h3 class="value-title">Autenticidad</h3>
            <p class="value-description">
              Creamos marcas genuinas que reflejan la verdadera esencia de cada
              cliente.
            </p>
          </article>
        </div>
      </section>
    </main>
  </div>
</BaseLayout>

<style>
  .about {
    display: flex;
    flex-direction: column;
    gap: 4rem;
  }

  .about-header-container {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  /* Intro Section */
  .about-intro {
    text-align: center;
    max-width: 65ch;
    margin: 0 auto;
  }

  .intro-text {
    font-size: var(--text-lg);
    color: var(--gray-300);
    line-height: 1.8;
  }

  /* Mission & Vision Section */
  .mission-vision-section {
    padding: 2rem 0;
  }

  .mv-grid {
    display: grid;
    gap: 2rem;
  }

  @media (min-width: 50em) {
    .mv-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 2.5rem;
    }
  }

  .mv-card {
    position: relative;
    background: var(--gradient-subtle);
    border: 1px solid var(--gray-800);
    border-radius: 1.5rem;
    padding: 2rem;
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    overflow: hidden;
    transition: all 0.5s cubic-bezier(0.22, 1, 0.36, 1);
  }

  .mv-card::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: var(--gradient-accent);
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.5s cubic-bezier(0.22, 1, 0.36, 1);
  }

  .mv-card:hover {
    transform: translateY(-8px);
    border-color: var(--accent-regular);
    box-shadow:
      0 20px 40px -15px rgba(118, 17, 166, 0.2),
      0 8px 20px -8px rgba(0, 0, 0, 0.15);
  }

  .mv-card:hover::before {
    transform: scaleX(1);
  }

  .mv-icon {
    width: 4rem;
    height: 4rem;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--accent-subtle-overlay);
    border-radius: 1rem;
    color: var(--accent-regular);
    transition: all 0.4s ease;
  }

  .mv-card:hover .mv-icon {
    background: var(--accent-regular);
    color: white;
    transform: scale(1.1) rotate(-5deg);
  }

  .mv-title {
    font-size: var(--text-2xl);
    font-weight: 700;
    color: var(--gray-0);
    margin: 0;
  }

  .mv-text {
    font-size: var(--text-md);
    color: var(--gray-300);
    line-height: 1.7;
    margin: 0;
  }

  .mv-text + .mv-text {
    margin-top: 1rem;
  }

  @media (min-width: 50em) {
    .mv-card {
      padding: 2.5rem;
    }
  }

  /* Values Section */
  .values-section {
    padding: 3rem 0;
  }

  .values-header {
    text-align: center;
    margin-bottom: 3rem;
  }

  .section-title {
    font-size: var(--text-3xl);
    font-weight: 700;
    color: var(--gray-0);
    margin: 0 0 0.75rem 0;
  }

  .section-subtitle {
    font-size: var(--text-lg);
    color: var(--gray-400);
    margin: 0;
  }

  .values-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.5rem;
  }

  @media (min-width: 600px) {
    .values-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 2rem;
    }
  }

  @media (min-width: 1000px) {
    .values-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .value-card {
    position: relative;
    background: var(--gradient-subtle);
    border: 1px solid var(--gray-800);
    border-radius: 1.25rem;
    padding: 2rem;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
    transition: all 0.4s cubic-bezier(0.22, 1, 0.36, 1);
    overflow: hidden;
  }

  .value-card::after {
    content: "";
    position: absolute;
    inset: 0;
    background: linear-gradient(
      135deg,
      transparent 0%,
      var(--accent-overlay) 100%
    );
    opacity: 0;
    transition: opacity 0.4s ease;
    pointer-events: none;
  }

  .value-card:hover {
    transform: translateY(-6px);
    border-color: var(--accent-regular);
    box-shadow:
      0 20px 40px -15px rgba(118, 17, 166, 0.25),
      0 8px 20px -8px rgba(0, 0, 0, 0.15);
  }

  .value-card:hover::after {
    opacity: 1;
  }

  .value-icon-wrapper {
    position: relative;
    z-index: 1;
  }

  .value-icon {
    width: 4rem;
    height: 4rem;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--accent-subtle-overlay);
    border-radius: 50%;
    color: var(--accent-regular);
    transition: all 0.4s cubic-bezier(0.22, 1, 0.36, 1);
  }

  .value-card:hover .value-icon {
    background: var(--accent-regular);
    color: white;
    transform: scale(1.15) rotate(10deg);
    box-shadow: 0 8px 20px rgba(118, 17, 166, 0.4);
  }

  .value-title {
    position: relative;
    z-index: 1;
    font-size: var(--text-xl);
    font-weight: 600;
    color: var(--gray-0);
    margin: 0;
  }

  .value-description {
    position: relative;
    z-index: 1;
    font-size: var(--text-sm);
    color: var(--gray-400);
    line-height: 1.6;
    margin: 0;
  }

  /* Animations */
  [data-animate] {
    opacity: 0;
    transform: translateY(30px);
    transition:
      opacity 0.6s cubic-bezier(0.22, 1, 0.36, 1),
      transform 0.6s cubic-bezier(0.22, 1, 0.36, 1);
  }

  [data-animate].animated {
    opacity: 1;
    transform: translateY(0);
  }

  [data-delay="1"] {
    transition-delay: 0.1s;
  }
  [data-delay="2"] {
    transition-delay: 0.2s;
  }
  [data-delay="3"] {
    transition-delay: 0.3s;
  }
  [data-delay="4"] {
    transition-delay: 0.4s;
  }
  [data-delay="5"] {
    transition-delay: 0.5s;
  }

  @media (min-width: 50em) {
    .about {
      gap: 5rem;
    }

    .section-title {
      font-size: var(--text-4xl);
    }

    .intro-text {
      font-size: var(--text-xl);
    }
  }
</style>

<script>
  // Intersection Observer for scroll animations
  document.addEventListener("DOMContentLoaded", () => {
    const animatedElements = document.querySelectorAll("[data-animate]");

    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            entry.target.classList.add("animated");
          }
        });
      },
      {
        threshold: 0.1,
        rootMargin: "0px 0px -50px 0px",
      },
    );

    animatedElements.forEach((el) => observer.observe(el));
  });
</script>
